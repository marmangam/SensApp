<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout vertical que representa el menu desplegable lateral de los administrativos. -->

<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_administrativos"
        layout="@layout/app_bar_administrativos"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:itemTextAppearance="@style/TextoMenu"
        app:headerLayout="@layout/nav_header_administrativos"
        app:menu="@menu/activity_administrativos_drawer" />
</androidx.drawerlayout.widget.DrawerLayout>
<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout vertical que representa el menu desplegable lateral de los operadores. -->

<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_operadors"
        layout="@layout/app_bar_operadores"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:itemTextAppearance="@style/TextoMenu"
        app:headerLayout="@layout/nav_header_operadores"
        app:menu="@menu/activity_operadores_drawer" />
</androidx.drawerlayout.widget.DrawerLayout>